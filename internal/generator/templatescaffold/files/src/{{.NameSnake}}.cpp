#include <cstdio>

// Ponto de entrada de {{.NamePascal}}. O nome deste arquivo
// (src/{{.NameSnake}}.cpp) também veio de um placeholder — nomes de
// arquivo/pasta são substituídos como qualquer outro conteúdo.

int main() {
    std::printf("Hello from {{.NamePascal}}!\n");
    return 0;
}
